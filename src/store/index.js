import Vue from 'vue'
import Vuex from 'vuex'
import sourceDate from '@/data.json'

Vue.use(Vuex)

export default new Vuex.Store({
  state: sourceDate,
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
